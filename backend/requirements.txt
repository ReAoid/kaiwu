# Core dependencies
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0.0

# Web tools dependencies
html2text>=2024.2.26

# Image processing (optional, for enhanced image analysis)
Pillow>=10.0.0

# OCR dependencies (optional)
pytesseract>=0.3.10

# PDF processing (optional, for PDF text extraction)
# PyMuPDF is preferred, pypdf is fallback
pymupdf>=1.24.0
pypdf>=4.0.0

# MCP (Model Context Protocol) support
mcp>=1.0.0

# Test dependencies
pytest
